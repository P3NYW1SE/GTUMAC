{
  "name": "gtu-live-platform",
  "version": "1.0.0",
  "scripts": {
    "build": "cd web && npm install && npm run build",
    "start": "cd server && npm install && npm start"
  },
  "engines": {
    "node": ">=18"
  }
}